/**
 * Metadata registry for Quick-action controls.
 *
 * Each entry must satisfy the schema defined in ui/metadata/schema.js.
 * Populate this array with control descriptor objects; the renderer
 * iterates it to build the Quick section of the cheat UI.
 *
 * @type {import('../schema.js').ControlMeta[]}
 */
export const quickMetadata = [];
